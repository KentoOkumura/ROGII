[project]
name = "kaggle-experiment-template"
version = "0.1.0"
description = "Kaggle experiment template workspace"
requires-python = ">=3.11"
dependencies = [
    "kaggle",
    "numpy",
    "pandas",
    "pyarrow",
    "pydantic",
    "pyyaml",
    "scikit-learn",
    "typer",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
notebook = [
    "ipykernel",
    "jupyterlab",
    "jupytext",
]
app = [
    "streamlit",
]
viewer = [
    "dtw-python>=1.3",
    "PySide6>=6.5,<6.9",
    "pyqtgraph>=0.13",
    "scipy>=1.10",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
    ".agents",
    "docs/notebooks",
    "*.ipynb",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
