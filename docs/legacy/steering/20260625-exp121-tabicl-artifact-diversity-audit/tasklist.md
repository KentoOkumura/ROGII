# タスクリスト

## TODO

- Kaggle 実行後、mount された TabICL / artifact source の inventory と pairwise を確認する。
- 候補が有効だった場合、`result.md` と `KAGGLE_DIRECTION.md` に採用可否を追記する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- 再現性設計を `design.md` に記入した。
- CPU-only / no RNG / no TabICL rerun の制約を `config.yaml` に反映した。
- source missing を正常な監査結果として記録する方針にした。
