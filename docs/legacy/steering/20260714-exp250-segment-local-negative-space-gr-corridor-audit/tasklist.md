# タスクリスト

## TODO

- なし。

## 進行中

- なし。

## ブロック中

- なし。

## 完了

- 現行実装契約を既存exp249から分離し、exp250へ固定。
- docs/06_reproducibility.mdを確認し、stable SHA、single-process、gzip SHA、upstream provenanceを設計へ記録。
- no-training cost、禁止事項、Stage 0/1 gateを固定。
- compact self-contained train/inference notebookとStage 0/1 orchestrationを実装。
- synthetic DAG/DP contract、py_compile、Ruff、Jupytext testをpass。
- strict experiment validationとproject template validationをpass。
- canonical Stage 0 Kaggle packageを作成し、CPU/internet off、2 kernel sources、embedded/loose/source config一致を確認。
- Stage 0 Kaggle CPU version 1を完了し、synthetic/input preflightと36 PNG / 72-row manifest SHAを確認。
- axis/grid/normalization/overlay、real/shuffled support/source/sink/candidateのmanual parityをpass。
- 同一固定configのStage 1 gateだけを有効化。
- Stage 1 push前に2 diagnostic surfaces / 0 config / 0 fold / 0 booster / parent retrainingなしを再確認。
- 同一固定configでStage 1 version 2を完了し、773 wells / 3,783,989 candidate rowsを監査。
- 8 guardを判定し、2 PASS / 6 FAIL、`fail_close_segment_local_hard_use_and_grid_search`を確定。
- backlogから実装済み項目を削除し、依存するsegment-local feature化を閉じる。
