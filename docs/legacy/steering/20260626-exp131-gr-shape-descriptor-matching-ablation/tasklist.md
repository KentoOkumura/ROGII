# タスクリスト

## TODO

- Kaggle train package を作成する。
- Kaggle train を実行する。
- output を取得し、summary / score metrics / rank metrics / bucket / by-well stress を記録する。
- 実行結果に応じて `KAGGLE_DIRECTION.md` の backlog を更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- 実験フォルダを exp099 からコピーして exp131 にリネームした。
- `config.yaml` を shape descriptor matching ablation 用に更新した。
- `gr_shape_descriptor_matching_ablation.py` を実装した。
- train / inference notebook を exp131 用に更新した。
- README / SESSION_NOTES / result / metrics の初期状態を更新した。
- 再現性設計を `design.md` に記入した。
