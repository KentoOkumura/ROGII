# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `experiments/exp213_prefix_structural_prior_pfbeam/` を exp211 からコピーした。
- helper / train / inference / config / README / result / metrics の exp213 化を実装した。
- structural expected TVT / delta / velocity を PF 初期速度、PF velocity pull、Beam absolute/step-delta soft cost に接続した。
- Beam top-K path と diagnostics を row candidates / PF diagnostics に保存する実装を追加した。
- 実行予定 active variant 数、fold 数、booster 数を `SESSION_NOTES.md` に記録した。
- Jupytext 変換と `--test` を通した。
- `py_compile` / `ruff --select F821` を通した。
- `make validate-exp EXP=exp213_prefix_structural_prior_pfbeam` を通した。
- Kaggle train package を `--strict` で生成した。
- 合成 well の active structural prior smoke で PF/Beam helper を通した。
- Kaggle train v1 を push し、完了後に logs / output を取得した。
- kernel id / runtime / output / SHA を `SESSION_NOTES.md` と `metrics.json` に追記した。
- `result.md` と `README.md` を完了結果で更新した。
- `KAGGLE_DIRECTION.md` と `experiment_summary.md` を最終結果で更新した。
