# タスクリスト

## ブロック中

- [ ] exp311とexp313が全gateをPASSする。

## 将来の実装（未着手）

- [ ] finite run AR(1) prior、shrinkage、fallback、innovation scoreを実装する。
- [ ] global/unpooled/group-shuffle controlsとrank guardを実装する。
- [ ] static validation後、別承認でKaggle CPU runを行う。

## 完了

- [x] AR order、support、shrinkage、clip、fallback、gateを固定した。
- [x] smoothing・decoder・grid・inference・submissionを禁止した。
