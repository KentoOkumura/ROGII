# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp046_hidden_branch_surrogate_audit` を `exp034` から作成した。
- `config.yaml` を見えない test well 用処理の代理監査用に更新した。
- `hidden_branch_surrogate_audit.py` を追加した。
- train / inference notebook を exp046 audit-only 構成に更新した。
- py_compile、ruff、validate-exp、notebook/scaffold tests を通した。
- `--max-wells 5` smoke を `/tmp/exp046_smoke` に出力した。
- Kaggle train package を生成した。
- Kaggle train version 1 を実行し、full surrogate audit を完了した。
- output を `experiments/exp046_hidden_branch_surrogate_audit/artifacts/` と `metrics.json` に同期した。
- Full 結果を `SESSION_NOTES.md` と `result.md` に記録した。
