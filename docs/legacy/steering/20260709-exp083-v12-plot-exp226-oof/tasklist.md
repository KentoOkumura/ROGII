# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- 要件と設計を記入した。
- exp083 v12 plot notebook source に exp226 OOF path resolution を追加した。
- exp226 OOF reader と `id,well` 結合を追加した。
- TVT plot、title、manifest、summary に exp226 OOF を追加した。
- `.ipynb` を Jupytext で再生成した。
- `py_compile`、`ruff --select F821`、`jupytext --to ipynb --test` が通った。
- Kaggle package notebook を同期し、`kernel-metadata.json` に `kentookumura/exp226-k16-kappa-repro-train` を追加した。
- local/package notebook と package metadata の JSON 検証、local/package notebook 同一性確認が通った。
- Kaggle v26 として push し、`KernelWorkerStatus.COMPLETE` と logs 上の 773 plots 生成完了を確認した。
