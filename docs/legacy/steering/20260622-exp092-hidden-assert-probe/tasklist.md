# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering requirements / design を作成した。
- `run_saved_model_inference()` に opt-in hidden assert probe を追加した。
- inference notebook から `inference.hidden_assert_probe` を渡すようにした。
- `config.yaml` に assert 条件を記録した。
- exp092 固有の label-free proxy check として `pred_delta`、near-prefix、projection correction、U-space disagreement の guard を追加した。
