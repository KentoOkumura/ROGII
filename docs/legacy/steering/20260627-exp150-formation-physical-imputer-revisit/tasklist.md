# タスクリスト

## TODO

- 必要に応じて Kaggle train を push して full OOF audit を実行する。
- full run 後、`SESSION_NOTES.md`、`result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` を結果で更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- exp138 から exp150 を作成した。
- 再現性設計を `design.md` に記入した。
- CPU-only / 0 booster 実験として `config.yaml` に記録した。
- fold-safe multi-formation physical imputer audit を実装した。
- train / inference notebook を exp150 用に更新した。
- ruff / py_compile / validate_experiment / 8-well smoke を通した。
- Kaggle train / inference package の strict prepare を通した。
- `uv run pytest` を通した。
- `experiment_summary.md` に exp150 を追加した。
