# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- 再現性設計を `design.md` に記入した。
- `experiments/exp134_self_gr_multiscale_longtail_gate/` を作成した。
- `config.yaml` を posthoc audit / no LightGBM training 方針に更新した。
- self-GR multiscale longtail gate audit script を実装した。
- train notebook で入力確認、audit 実行、metrics preview、summary 保存を追えるようにした。
- inference notebook は no-submission summary にした。
- `result.md`、`README.md`、`metrics.json`、`SESSION_NOTES.md` を初期化した。
- `uv run python scripts/validate_experiment.py --experiment exp134_self_gr_multiscale_longtail_gate` を通した。
- py_compile、notebook JSON、ruff check、ruff format check を通した。
- Kaggle train package を作成し、metadata と bootstrap manifest の config / script SHA を確認した。
- Kaggle train v1 を実行し、output を取得した。
- feature content SHA、diagnostic prediction SHA、主要 metrics、棄却判断を記録した。
