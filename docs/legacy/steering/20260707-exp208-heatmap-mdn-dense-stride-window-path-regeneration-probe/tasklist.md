# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `experiments/exp208_heatmap_mdn_dense_stride_window_path_regeneration_probe/` を exp207 から作成した。
- `config.yaml` を exp202 saved model dense stride regeneration 用に更新した。
- dense stride path regeneration helper を実装した。
- train / inference notebook source を exp208 用に更新した。
- README / SESSION_NOTES / result / metrics を初期状態に更新した。
- Jupytext train / inference conversion and `--test` を通した。
- `py_compile`、`ruff --select F821,E501`、`make validate-exp` を通した。
- Kaggle train push 前に、CPU diagnostic、CNN training 0、LightGBM 0 configs / 0 boosters、parent/control retraining なしを再確認した。
- Kaggle train v1 `kentookumura/exp208-hmdn-dense-stride-train` が COMPLETE。
- output を `experiments/exp208_heatmap_mdn_dense_stride_window_path_regeneration_probe/kaggle/output/train_v1` に取得した。
- README / SESSION_NOTES / result / metrics / experiment_summary を完了結果に更新した。
- `KAGGLE_DIRECTION.md` の実装済み backlog 行を削除した。
