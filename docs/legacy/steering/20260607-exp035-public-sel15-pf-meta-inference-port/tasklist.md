# タスクリスト

## TODO

- Kaggle inference package を生成する。
- Kaggle inference を実行し output artifact を取得する。
- `submission.csv` の submit-check、range、diff、changed wells を記録する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- exp035 steering docs を作成した。
- exp033 inference port から exp035 実験フォルダを作成した。
- exp034 の `baseline.py` / `pseudo_tail_augmentation.py` を同梱した。
- config を exp034 selected meta inference port 用に更新した。
- inference notebook に exp026 anchor fit、sampled-row anchor regeneration、Ridge meta residual fit、見えない test well 用補正 を実装した。
- train notebook を inference-only guard に更新した。
