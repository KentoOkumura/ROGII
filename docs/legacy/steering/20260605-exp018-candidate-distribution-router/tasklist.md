# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- `exp018_candidate_distribution_router` の steering docs を作成した。
- exp016 から実験フォルダを作成し、exp018 用に notebook 名と `settings.py` を更新した。
- `config.yaml` に source candidates、distance buckets、router candidates を定義した。
- `audit_candidate_distribution_router.py` を実装した。
- README、SESSION_NOTES、result、metrics stub を exp018 用に更新した。
- OOF router audit を実行し、結果を `SESSION_NOTES.md` / `result.md` / `metrics.json` に記録した。
- Same-OOF 改善はあるが fold 外 selection が悪化するため、router は提出実装へ進めない判断を記録した。
- 実行結果に応じて `experiment_summary.md` と `KAGGLE_DIRECTION.md` を更新した。
