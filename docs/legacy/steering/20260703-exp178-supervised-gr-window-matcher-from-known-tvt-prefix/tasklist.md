# タスクリスト

## TODO

- real GR が shuffled/no-GR を上回った場合だけ、PF/Beam observation likelihood feature または exp148/exp092 add-only confidence feature の follow-up を検討する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp178_supervised_gr_window_matcher_from_known_tvt_prefix` を作成した。
- `config.yaml` に route、leakage policy、再現性、pair dataset、decision thresholds を記録した。
- Jupytext percent 形式の train/inference script を実装した。
- GPU/booster コストを `SESSION_NOTES.md` に記録した。
- train notebook の Kaggle package を canonical kernel id/title で prepare した。
- Kaggle train v1 を `kentookumura/exp178-supervised-gr-window-matcher-train` で実行した。
- Kaggle output を取得し、`SESSION_NOTES.md`、`result.md`、`metrics.json`、`experiment_summary.md` を実測値で更新した。
