# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp225_state_known_tvt_self_gr_hmm_emission` を `exp223_joint_typewell_self_gr_hmm_likelihood_probe` から作成した。
- `state_known_tvt_curve` self-GR emission surface を実装した。
- active variant / LightGBM config / fold / booster count を config と SESSION_NOTES に記録した。
- 再現性設計を `design.md` に記入した。
- `py_compile`、`ruff --select F821`、`validate_experiment.py`、Jupytext 変換と `--test` を通した。
- synthetic state mask unit check を通し、known TVT 範囲外 state が neutral になることを確認した。
- Kaggle train push 前に metadata と bootstrap 内 config の整合を確認した。
- Kaggle train v1 を実行し、`KernelWorkerStatus.COMPLETE` を確認した。
- logs / output archive から metrics、feature content SHA、runtime、worst-well regression を記録した。
- `result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` を更新した。
- 判定は完了・不採用。raw-test regeneration、inference、submit は行わない。
