# タスクリスト

## TODO

- Kaggle train notebook を prepare/push して full audit を実行する。
- Kaggle output を同期し、`SESSION_NOTES.md`、`result.md`、`metrics.json`、`experiment_summary.md` を更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成する。
- `exp041_ravaghi_beam_exact_feature_ablation` を `exp040` から作成する。
- exact Ravaghi beam feature family を `config.yaml` に定義する。
- audit script に exact beam path 生成と feature injection を実装する。
- README、SESSION_NOTES、result、metrics placeholder を `exp041` 用に更新する。
- ruff、py_compile、`scripts/validate_experiment.py` を通す。
- 5 wells / 11,207 rows の local smoke で exact beam 生成と小規模 audit を確認する。
