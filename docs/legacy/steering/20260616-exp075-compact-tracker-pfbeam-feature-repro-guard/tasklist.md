# タスクリスト

## TODO

- Optional: code submission を行う場合は Public LB と ref を記録する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- exp074 から exp075 を作成した。
- PF/Beam train feature generation notebook を追加した。
- LightGBM train notebook を generated feature CSV 読み込み構成に変更した。
- inference notebook を exp075 naming に更新した。
- LightGBM feature importance CSV と matplotlib PNG 保存を実装した。
- 静的検証を完了した。
- `prepare_kaggle_notebooks.py` に `pfbeam_features` kind を追加した。
- `pfbeam_features` / `train` / `inference` の Kaggle package 生成を strict で確認した。
- PF/Beam feature generation v3 を CPU notebook として実行完了した。
- PF/Beam feature generation v3 の output、summary、log、SHA を記録した。
- Kaggle train v1 を実行完了した。
- Kaggle train v1 の CV、feature source SHA、model manifest SHA、feature importance PNG SHA を記録した。
- Kaggle inference v1 を実行完了した。
- Kaggle inference v1 の raw-test feature SHA、prediction SHA、submission SHA、submit-check PASS を記録した。
