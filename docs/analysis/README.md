# `docs/analysis`の扱い

このディレクトリは旧形式の分析文書を保存している場所です。新しい完了レポートは追加せず、`docs/surveys/`へ保存して`docs/surveys/README.md`から検索できるようにします。

既存文書は一括移動しません。次に内容を更新または再利用するとき、根拠と参照先を確認して`docs/surveys/`の既存レポートへ統合するか、metadata付きの新規surveyレポートとして移します。内容が不明な箇所を推測で補完しません。
