# タスクリスト

## TODO

- なし。

## 進行中

- なし。

## ブロック中

- なし。

## 完了

- backlogをsteeringへ切り出した。
- 再現性設計を`design.md`に記入した。
- `exp254`のconfig、Jupytext train/inference、README、SESSION_NOTES、result、metricsを作成した。
- exp243 exact PF reference、legacy single-seed loop、Numba all-seed kernelを実装した。
- representative-well選択、seed grid、candidate spec grid、分離計時、RAM計測を実装した。
- seed bank cache、warm aggregation、content SHA、repeat / round-trip guardを実装した。
- probe gateを満たさないfull workloadをfail-closedにした。
- synthetic contract test、py_compile、Ruff、Jupytext round-trip、strict exp/template validationを通した。
- Kaggle metadata、loose package、bootstrap manifest内config/sourceのSHA一致を確認した。
- `KAGGLE_DIRECTION.md`を未着手backlogから実装済み・Kaggle train待ちへ移した。
- `experiment_summary.md`を更新した。
- Kaggle CPU probe v1を実行し、kernel version / id、runtime、peak RAM、workloadを記録した。
- trajectory / log-likelihood / mean exact、exp243保存値、repeat / cache / warm SHAの全guard通過を確認した。
- probe summary SHAを実行証拠として記録し、projectionと実測full runtimeを分離した。
- 実用的な高速化と現在の用途がないため不採用とし、full workload gateを再度fail-closedにした。
- ユーザー指示によりbranchを閉じ、追加実験・inference・submissionへ進まないことを記録した。
