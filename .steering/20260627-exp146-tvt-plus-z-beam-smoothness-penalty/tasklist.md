# タスクリスト

## TODO

- Kaggle train package を作成して実行する。
- output 取得後に `result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` を更新する。
- 改善した場合だけ raw-test-compatible inference port を同じ exp146 内で設計する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp140` から exp146 を作成した。
- exp142 から誤混在した `tvt_plus_z_beam_smoothness_penalty` 実装を除去した。
- Beam search cost に `U` / `dU/dMD` / `dU` curvature penalty を追加する実装に差し替えた。
