# タスクリスト

## TODO

- Kaggle train に push して full audit を実行する。
- output 取得後に `candidate_metrics.csv`、summary、SHA、解釈を `SESSION_NOTES.md` / `result.md` / `metrics.json` に記録する。
- exp072 `likpf_mean` / `likpf_scale_*` に勝つ候補がある場合だけ、同じ exp103 内で inference port を検討する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp103_pf_z_xy_likpf_ensemble_parity` を exp100 から作成した。
- `pf_z_xy_likpf_ensemble_parity.py` を実装した。
- `config.yaml` を exp103 parity audit 用に更新した。
- train / inference notebook を exp103 名で作成した。
- stochastic PF の stable seed policy と SHA 記録方針を設計に明記した。
- `py_compile`、notebook JSON、ruff、`validate-exp` を通した。
- Kaggle train package を strict で生成した。
