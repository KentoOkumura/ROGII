# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp223_joint_typewell_self_gr_hmm_likelihood_probe` を作成した。
- `config.yaml` を train-side no-training self-GR HMM readout に更新した。
- self-GR descriptor matching surface と clipped HMM emission variants を実装した。
- direct comparison に self-GR signal bucket metrics を追加した。
- Jupytext で train / inference notebook を再生成した。
- `py_compile`、`ruff --select F821`、`json.tool`、`validate_experiment.py`、Jupytext test を通した。
- 初回 Kaggle run を `boost_only` / `alpha=[0.07, 0.15]` の 2 HMM variants に絞った。
- Kaggle train v1 を push し、`KernelWorkerStatus.RUNNING` を確認した。
- Kaggle train v1 完了を確認し、metrics、SHA、result、experiment_summary、KAGGLE_DIRECTION を更新した。
