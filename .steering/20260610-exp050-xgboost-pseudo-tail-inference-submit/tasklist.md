# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成する。
- `exp026` から `exp050` を作成する。
- `settings.py`、notebook 名、config、README、SESSION_NOTES、result、metrics を exp050 用に更新する。
- `baseline.py` に `XGBRegressor` 対応を追加する。
- pseudo cutoff 生成の read-only view 対策を入れる。
- 静的チェックと strict validation を通す。
- Kaggle inference notebook を prepare する。
- Kaggle inference notebook を push して output を取得する。
- submit-check、予測範囲、exp026 差分を記録する。
- Kaggle output の小さい生成物を `artifacts/` に同期する。
- code competition submit の完了を確認し、Public LB と ref を記録する。
