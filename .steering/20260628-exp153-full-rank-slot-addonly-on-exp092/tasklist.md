# タスクリスト

## TODO

- Colab high-memory runtime で runner notebook を開き、layout validation と LightGBM GPU smoke を実行する。
- Full train を開始し、`colab_runs/latest_run.json` と log を監視する。
- 完了後に pooled OOF、by-well、bucket、feature importance、feature schema、model manifest を確認する。
- exp092 / exp098 / exp139 / exp147 と比較して inference port の可否を判断する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- 実験フォルダを作成した。
- config を full rank-slot add-only に更新した。
- Kaggle train/inference notebook を新実験名に更新した。
- Colab train runner notebook を追加した。
- GPU コストガードを `SESSION_NOTES.md` / `metrics.json` / `result.md` に記録した。
