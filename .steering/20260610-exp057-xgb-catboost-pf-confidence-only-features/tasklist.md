# タスクリスト

## TODO

- Kaggle train を実行し、metrics / artifacts を取得する。
- 結果を `SESSION_NOTES.md`、`result.md`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` に記録する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- `.steering/20260610-exp057-xgb-catboost-pf-confidence-only-features/` を作成した。
- `experiments/exp057_xgb_catboost_pf_confidence_only_features/` を `exp055_single_model_pseudotail_training` から作成した。
- `config.yaml` を XGBoost / CatBoost の geometry control と PF confidence-only variant に更新した。
- `pf_confidence_model_audit.py` を exp057 用に更新した。
- train / inference notebook を exp057 名と `pf_confidence_*` artifacts に更新した。
- static check、required column check、`validate-exp` を通した。
- Kaggle train package を作成した。
