# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering 要件を記入した。
- 再現性設計を `design.md` に記入した。
- exp220 実験ディレクトリを作成した。
- row-neighbor context feature generator を実装した。
- CPU `train_lgb0` / `train_lgb1` / `train_lgb2` notebook source を追加した。
- Jupytext 変換と `--test` を通した。
- `py_compile` と `ruff --select F821,F401` を通した。
- `validate_experiment.py` strict validation を通した。
- `train_lgb0` / `train_lgb1` / `train_lgb2` の Kaggle package prepare を `--strict` で通した。
- Kaggle `train_lgb0` / `train_lgb1` / `train_lgb2` を push / 実行し、すべて COMPLETE を確認した。
- split outputs の metrics / manifest / prediction SHA を記録した。
- split outputs から `lgb_mean` を streaming aggregate し、CV / bucket / by-well / feature importance を記録した。
- exp220 は現行 anchor に届かないため、inference package 作成と submit は行わない判断にした。
