# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成。
- `exp021` から `exp022_distance_uncertainty_shrink` を作成。
- config / settings / README / SESSION_NOTES / result / metrics を exp022 用に更新。
- uncertainty shrink の固定 3 候補と OOF proxy column 保存を実装。
- Python compile / ruff / validate-exp / notebook code compile を通過。
- Kaggle train / inference notebook の prepare を通過。
- Kaggle train を push し、output を取得。
- CV と bucket 別 metrics を記録。
- fixed uncertainty shrink は親 `weighted_distance_bucket_shrink` を上回らなかったため、inference / submit を見送り。
- `experiment_summary.md` と `KAGGLE_DIRECTION.md` を結果に合わせて更新。
