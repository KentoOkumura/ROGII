# タスクリスト

## TODO

- Kaggle train を push する場合は、生成 notebook metadata と bootstrap config の一致を確認する。
- Kaggle train 完了後、OOF、bucket、worst-well、feature importance、prediction/model SHA を `SESSION_NOTES.md` と `result.md` に記録する。
- 改善した場合だけ raw-test/full-train feature parity と inference port を同じ exp 内で検討する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs 作成。
- exp151 ディレクトリ作成。
- `config.yaml` に route、lineage、feature groups、GPU cost guard を記録。
- `tvt_dense_addonly_confidence_features_on_exp092.py` に dense confidence feature builder と train orchestration を実装。
- train/inference notebook、README、SESSION_NOTES、result、metrics を exp151 用に更新。
