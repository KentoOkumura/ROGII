# タスクリスト

## TODO

- Kaggle train を push する。
- Kaggle train 実行後、logs / cell output を根拠に CV、best policy、worst-well、exp115 stress を記録する。
- 結果に応じて `KAGGLE_DIRECTION.md` の backlog を整理する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- exp118 から exp175 の実験ディレクトリを作成した。
- `config.yaml` を cluster-outlier gated prior audit 用に更新した。
- 補助スクリプト `cluster_outlier_typewell_prior_gate.py` を実装した。
- Jupytext 起点の train / inference `.py` を追加した。
- 構文チェックと ruff の未定義名・未使用 import・長い行チェックを通した。
- Jupytext train / inference notebook 変換と `--test` を通した。
- `make validate-exp EXP=exp175_cluster_outlier_typewell_prior_gate` を通した。
- Kaggle train / inference package を canonical kernel id 付きで生成した。
- Kaggle train push 前の実行予定数を `SESSION_NOTES.md` に記録した。
