# タスクリスト

## TODO

- Kaggle `train_lgb0` package を prepare/push する。
- Kaggle `train_lgb1` package を prepare/push する。
- Kaggle `train_lgb2` package を prepare/push する。
- 3 split の fold metrics / pooled metrics を統合して result / experiment_summary / KAGGLE_DIRECTION を更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- 実験 directory を作成した。
- exp191 continuity selector confidence replacement-only helper を実装した。
- CPU split train 起点 `train_lgb0` / `train_lgb1` / `train_lgb2` を追加した。
- 再現性設計を `design.md` と `config.yaml` に記録した。
