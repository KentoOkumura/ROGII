# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- full-OOF Stage 1、current-test、inference、submission:
  Stage 0 technical FAILのterminal decisionにより禁止。

## 完了

- conditional backlog項目を作成した。
- exp406実験scaffoldを作成した。
- steering requirements / design / tasklistを確定した。
- fixed16、GR edge、loop closure、negative control、prefix gate、
  resource、停止条件をconfigへ固定した。
- 実装を開始していないことを確認した。
- exp405のtechnically-valid scientific FAILとdecision SHA
  `e159cfb712a6ed81e78f4524febbf0d995375124a473a5056aad3c1347b648f0`
  を確認し、Stage 0を解禁した。
- ユーザーからexp406 Stage 0実装承認を得た。
- 保存済みexp226 OOFがprefix行を持たないことを確認し、ユーザー承認済みの
  fixed16 pseudo-cut K16 geometry replayへcontrol契約を更新した。
- 10章・3,000行超のcompact self-contained train候補と、
  fail-closed inference候補をJupytext percent形式で実装した。
- exp386同型fixed16 selector、fold-safe donor、native-overlap Type-Well優先、
  pairwise edge/rejection funnel、circular control、fundamental cycle、
  Huber IRLS、target-free freeze、prefix replay、resource/全AND gateを実装した。
- 専用test 13件、py_compile、Ruff F821/F811、Jupytext train/inference
  round-trip、strict experiment validationをPASSした。
- ユーザーから固定量のKaggle CPU Stage 0、正規train Notebook採用、
  package、push、runの明示承認を得た。
- 正規train Notebookを採用し、package metadata / embedded config / SHAを監査した。
- canonical kernel version 1（id_no `128637170`）をpushし、CPU実行を開始した。
- version 1 COMPLETEを確認し、technical gate 12/15 PASS、
  graph coverage / finite row coverage / runtimeの3項目FAILを記録した。
- target-free gateでprefix replay 0、unknown suffix prediction 0を確認した。
- edge / cycle / gauge / read-ledgerを含む小規模生成物を保存し、
  manifest file SHAを8/8照合した。
- decision `close_exp406_without_parameter_rescue`に従ってrun authorizationを無効化し、
  parameter rescue、full OOF、current-test、inference、submissionなしでbranchを閉じた。
