# タスクリスト

## TODO

- Kaggle train audit を実行し、metrics / by-well / bucket / common-worst / parity を記録する。
- 実行後に `result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` を結果で更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- exp154 実験フォルダを exp135 から作成した。
- backlog の比較対象を exp148 anchor に更新する方針を反映した。
- `config.yaml` を no-new-LGBM posthoc verifier audit に更新した。
- 実装を exp148 `lgb_mean` base に変更した。
- near guard と candidate path continuity guard を追加した。
- GPU 学習なし、total boosters 0 を `SESSION_NOTES.md` と `metrics.json` に記録した。
