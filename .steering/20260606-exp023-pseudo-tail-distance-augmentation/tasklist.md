# タスクリスト

## TODO

- selected pseudo-tail recipe の inference/postprocess 実験を切り出す。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成する。
- `exp020` から `exp023_pseudo_tail_distance_augmentation` を作成する。
- config / README / notes / result / metrics を exp023 用に更新する。
- pseudo-tail augmentation audit script を実装する。
- train / inference notebook を exp023 用に更新する。
- validation / static checks / Kaggle package generation を通す。
- Kaggle train を push して full CV を取得する。
- 結果を `SESSION_NOTES.md`、`result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` に記録する。
