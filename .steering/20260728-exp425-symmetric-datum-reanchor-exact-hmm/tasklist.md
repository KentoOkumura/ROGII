# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- Stage 1、inference、submissionはStage 0 scientific / runtime gate FAILにより
  閉鎖。gate緩和やsame-sample rescueでは再開しない。

## 完了

- exp408 / exp412の機構証拠を確認した。
- rate方向をdatum方向へ直接写像しない根拠を保存ledgerで診断した。
- route、親、対称3枝、branch prior、shift、single-event、exact recursionを固定した。
- Stage 0 sample、実行量、technical / mechanism / runtime gateを固定した。
- 再現性、リーク、multiplicity、runtime、禁止事項を設計した。
- steering 3文書を作成した。
- design-only実験scaffold、config、README、SESSION_NOTES、result、metricsを作成した。
- train / inference NotebookをMarkdown-only placeholderとして固定した。
- `KAGGLE_DIRECTION.md`の未着手backlogと`experiment_summary.md`へ登録した。
- strict experiment validationとproject template validationをPASSした。
- ユーザーが`exp425_symmetric_datum_reanchor_exact_hmm`の実装対象を明示した。
- compact self-contained train / inference Jupytext sourceを実装した。
- unchanged first pass、first persistent event、対称3枝のconditional exact-HMM
  factorization、full-sequence evidence soft marginalizationを実装した。
- parent-only parity、独立exp209 parity、single-event、branch marginalization、
  truth-late freeze、inference fail-closeの専用testを実装した。
- 正規train / inference Notebook採用をconfigへ記録した。
- ユーザーの「実行してください」によりStage 0 package / push / runを明示承認した。
- Kaggle push前にbaseline / treatment well-runs、branch state数、model / booster数、
  parent control再実行理由を`SESSION_NOTES.md`へ再記録した。
- canonical Kaggle private CPU version 1（id_no `128930925`）を完了まで監視した。
- fixed32のtechnical gateを12 / 13、mechanism gateを3 / 7 PASSと判定し、
  `stage0_fail_closed`で閉じた。
- output実ファイルを必要時だけ取得し、first-pass message、event schedule、
  branch posterior、prediction、metrics、summaryのSHAを照合・記録した。
- Stage 0が全gate PASSではないため、Stage 1、inference、submissionへ進まないことを
  確定した。
