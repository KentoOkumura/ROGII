# タスクリスト

## ブロック中

- [ ] exp311とexp313が全gateをPASSする。

## 将来の実装（未着手）

- [ ] canonical descriptor、robust scaler、stable nearest-group tableを実装する。
- [ ] nested group-out、permutation control、far fallback guardを実装する。
- [ ] PASS時だけexp313 fallback契約の更新を提案する。

## 完了

- [x] similarity contract、cutoff、fallback、gate、禁止入力を固定した。
