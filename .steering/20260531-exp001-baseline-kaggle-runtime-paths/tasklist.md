# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering 作成。
- `settings.py`、notebook、workflow docs を Kaggle 実行前提に更新。
- `scripts/execute_experiment_notebook.py` を `--allow-local` 明示必須に変更。
- `project.yml` の `metadata.owner` から Kaggle kernel id を補完するよう `prepare_kaggle_notebooks.py` を更新。
- Kaggle prepared notebook を再生成し、strict metadata を確認。
- `validate_project.py`、`validate_experiment.py`、`pytest` が通過。
