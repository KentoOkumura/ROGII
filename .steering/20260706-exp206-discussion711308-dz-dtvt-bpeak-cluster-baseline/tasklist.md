# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- v4: test/query well の known `TVT_input` 全体で `dTVT ~= a*dZ+b` を fit し、unknown suffix を累積予測する `known_tvt_fit_full` を追加して selected に切り替える。
- 再現性設計を `design.md` に記入する。
- steering requirements / design / tasklist を作成する。
- 実験 scaffold を作成する。
- `config.yaml`、helper、train/inference Jupytext source、notebook、記録ファイルを実装する。
- `py_compile`、`ruff check`、Jupytext round-trip、`validate-exp`、Kaggle package prepare を通す。
- Kaggle train v1 / inference v1 / submission ref `54395246` を完了し、Public LB 41.214 を記録する。
- v1 要件未達を受け、step-fit / feature-nearest / prefix-holdout selector の v2 を実装する。
- Kaggle train v2 / inference v2 / submission ref `54396544` を完了し、Public LB 34.908 を記録する。
- `metrics.json`、`result.md`、`README.md`、`experiment_summary.md`、`submissions/SUBMISSIONS.md`、`KAGGLE_DIRECTION.md` を v2 要件未達の結論で更新する。
