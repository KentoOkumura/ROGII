# タスクリスト

## TODO

- Kaggle train notebook を push して実行する。
- output を取得し、candidate metrics / context stats / metrics.json を確認する。
- 結果に基づき `result.md`、`SESSION_NOTES.md`、`KAGGLE_DIRECTION.md`、`experiment_summary.md` を更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `experiments/exp123_cross_test_prefix_label_context_audit` を作成した。
- `config.yaml` に route、検証方針、rules-risk policy、audit parameters を記録した。
- `cross_test_prefix_label_context_audit.py` を実装した。
- train notebook を入力確認、監査実行、生成物 preview の構成にした。
- inference notebook を submission を作らない guard にした。
- 再現性設計を `design.md` に記入した。
- py_compile / ruff / experiment validation を通した。
- Kaggle train notebook directory を `experiments/exp123_cross_test_prefix_label_context_audit/kaggle/train` に生成した。
