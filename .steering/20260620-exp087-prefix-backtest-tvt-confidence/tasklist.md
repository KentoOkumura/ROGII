# タスクリスト

## TODO

- Kaggle train notebook を push / 実行し、output を取得する。
- 取得後に `SESSION_NOTES.md`、`result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` を実測値で更新する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `exp087_prefix_backtest_tvt_confidence` を作成した。
- `config.yaml` を diagnostic-only confidence calibration 設定へ更新した。
- `prefix_backtest_tvt_confidence.py` を実装した。
- train / inference notebook を更新した。
