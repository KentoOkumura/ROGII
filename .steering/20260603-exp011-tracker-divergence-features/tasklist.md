# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成する。
- exp010 から exp011 experiment folder を作成する。
- notebook 名と experiment metadata を exp011 に更新する。
- deterministic tracker features を `baseline.py` に追加する。
- tracker ablation variants を `config.yaml` に定義する。
- train notebook に `tracker_group_summary.csv` 出力を追加する。
- `validate_project.py`、`validate_experiment.py`、py_compile、ruff、pytest を通す。
- Kaggle train / inference notebook を strict prepare する。
- Kaggle train notebook を push して full CV を取得する。
- Kaggle output の `metrics.json`、`ablation_metrics.csv`、`well_metrics.csv`、`tracker_group_summary.csv` を反映する。
- 結果を `SESSION_NOTES.md` と `result.md` に記録する。
- `KAGGLE_DIRECTION.md` と `experiment_summary.md` の最終更新を確認する。
