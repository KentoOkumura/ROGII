# タスクリスト

## TODO

- Kaggle 上で full CV を実行し、target spec ごとの RMSE / bucket / well metrics / SHA を `SESSION_NOTES.md` と `metrics.json` に記録する。
- 結果に応じて Stage 2 の target subset または inference port を判断する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- exp073 から `exp080_u_space_target_ablation` を scaffold した。
- 再現性設計を `design.md` に記入した。
- target ablation 実装、notebook、config、記録ファイルを更新した。
- `validate_experiment.py`、ruff、notebook JSON validation を通した。
- Kaggle train package を生成した。
