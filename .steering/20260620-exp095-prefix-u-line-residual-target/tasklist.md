# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成した。
- `experiments/exp095_prefix_u_line_residual_target/` を作成した。
- `prefix_u_line_residual_target.py` に prefix U-line residual target と inverse transform を実装した。
- `config.yaml` に active targets、prefix-line fit / fallback 条件、leakage policy を記録した。
- train / inference notebook を exp095 用に更新した。
- Python compile、notebook JSON、ruff、`validate_experiment.py` を通した。
- Kaggle train package を生成し、metadata と bootstrap 内 config が exp095 を指すことを確認した。
- Kaggle train v1 を push / 実行し、COMPLETE を確認した。
- metrics、by-well、bucket、target summary、feature schema、model manifest、summary を取得した。
- `result.md`、`metrics.json`、`SESSION_NOTES.md`、`KAGGLE_DIRECTION.md` を更新した。
- `prefix_u_line_residual_target` は `dTVT` より大幅悪化したため inference port しないと判断した。
