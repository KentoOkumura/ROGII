# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering 作成。
- `exp015_public_pf_beam_scale_selector_features` から `exp017_deterministic_dtw_addonly` を作成。
- notebook 名と `settings.py` を exp017 に更新。
- `config.yaml` を exp017 用に更新し、control と DTW/DWT feature variants を定義。
- `baseline.py` に deterministic DTW/DWT feature generator を追加。
- validation / lint / compile / pytest を通過。
- train/inference Kaggle notebook を `--strict` で準備できることを確認。
- train Kaggle notebook を `--run-on-push --strict` で準備できることを確認。
- `experiment_summary.md` に in-progress 実験として追加。
- Kaggle full CV を実行して metrics/artifacts を取得。
- `SESSION_NOTES.md`、`result.md`、`README.md`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` に結果を反映。
