# タスクリスト

## TODO

- Kaggle CPU train package を prepare する。
- push 前に metadata と bootstrap 内 config の `enable_gpu=false` を確認する。
- Kaggle CPU train を実行する場合は logs から OOF / feature count / elapsed を記録する。
- train 完了後に OOF、near-row、`1000_plus`、worst-well、feature importance、raw-test/current-test parity を記録する。

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs 作成。
- exp148 から exp161 を scaffold。
- `prefix_crop_window_features_on_exp148.py` を作成。
- `prefix_crop_window` feature builder を実装。
- config を CPU active mode に更新。
- README / SESSION_NOTES / result / metrics を未実行状態に更新。
