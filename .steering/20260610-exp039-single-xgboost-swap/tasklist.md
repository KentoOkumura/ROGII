# タスクリスト

## TODO

- なし

## 進行中

- なし

## ブロック中

- なし

## 完了

- steering docs を作成。
- `exp038` から `experiments/exp039_single_xgboost_swap/` を作成。
- candidate estimator を `XGBRegressor` に差し替え。
- train / inference notebook 名と参照スクリプト名を exp039_single_xgboost_swap 用に更新。
- audit-only inference notebook として維持。
- `ruff`、`py_compile`、`validate_experiment` を通した。
- HGB override の小さい local smoke を通した。
- Kaggle train package を生成した。
- Kaggle train version 1 を完了し、`single_xgboost_*` 生成物を取得した。
- `SESSION_NOTES.md`、`result.md`、`metrics.json`、`experiment_summary.md`、`KAGGLE_DIRECTION.md` を結果で更新した。
- selected XGBoost candidate が exp039 LightGBM anchor と public PF controls を下回ったため、inference port しない方針にした。
