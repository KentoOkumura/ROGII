# exp325 結果

## 状態

親exp323のterminal closeにより閉鎖済み。未実装・未実行で、window separability、HMM RMSE、runtime結果はない。

## 固定した判定

Stage 0はMRR/top3各+0.01、親改善4/5 folds、real>shuffle 5/5 folds、1000+/hidden-like正、eligible window 25%以上を要求する。Stage 1は親比0.05 ft、4/5 folds、tail/p95/worst非悪化を要求する。

## 次

本実験は再開しない。新exp325相当はexp338 successor chainの二段階PASS後に新番号でのみ作成できる。
