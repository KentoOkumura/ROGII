# exp327 結果

## 状態

親exp323のterminal closeにより閉鎖済み。未実装・未実行で、NLL、RMSE、runtime結果はない。

## 固定した判定

Stage 0はNLL 1%以上、4/5 folds、平均sigma増分0.01 ft以上、upper clip 25%以下、coverage 100%。Stage 1は親比0.05 ftと通常のtail guardを要求する。

## 次

本実験は再開しない。新exp327相当はexp338 successor chainの二段階PASS後に新番号でのみ作成できる。
