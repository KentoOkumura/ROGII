experiment:
  name: exp071_exp063_full_replay_reproducibility_guard
  description: exp063 full replayの再現性設定を検証するために作成したが、実装前にexp072へ移行したscaffold。
  route: ml_model
  created_at: "2026-06-14"

lineage:
  parent: exp063_ravaghi_vs_pixiux_lgbm_feature_parity_audit
  diff_summary: 再現性設定だけを変更する計画だったが、CPU-only feature cacheを作るexp072へ移行したため実装・実行していない。

validation: {}
data: {}

model:
  name: exp063_full_replay_reproducibility_guard_not_implemented
  params: {}

runtime:
  use_amp: false
  num_workers: 2
  batch_size: 32

notes:
  - 実装、検証、Kaggle実行は行っていない。
  - 後続はexp072_exp063_full_replay_feature_cacheを参照する。
