# exp071_exp063_full_replay_reproducibility_guard セッションノート

## 現在の状態

- status: `discarded`（旧`abandoned_before_implementation`を現行statusへ正規化）

## メモ

exp070 の feature input mismatch 修正として一度 scaffold したが、方針を「再利用可能な CPU-only train feature cache を作る」へ切り替えたため、実装・検証・Kaggle 実行は行わない。

後続は `exp072_exp063_full_replay_feature_cache` を使う。
