# ---
# jupyter:
#   kernelspec:
#     display_name: Python 3
#     language: python
#     name: python3
# ---
# %% [markdown]
# # exp169_tvt_input_pfbeam_offset_calibration inference

# %% [markdown]
# This experiment is a train-side PF/Beam offset calibration audit. No inference candidate
# is selected at implementation time.

# %%
from __future__ import annotations

from IPython.display import display

from settings import ExperimentPaths, get_nested, load_config

paths = ExperimentPaths()
config = load_config()
paths.require_kaggle_runtime()

display(
    {
        "experiment": get_nested(config, "experiment.name"),
        "route": get_nested(config, "experiment.route"),
        "inference_mode": get_nested(config, "inference.mode"),
        "selected_candidate": get_nested(config, "inference.selected_candidate"),
        "note": "No submission is generated by this diagnostic notebook.",
    }
)
