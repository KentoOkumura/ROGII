# exp324 結果

## 状態

親exp323のterminal closeにより閉鎖済み。未実装・未実行で、精度、calibration、runtime結果はない。

## 固定した判定

Stage 0はtransition NLL 1%以上、4/5 folds、coverage/tail/fallback/clip gateを要求する。Stage 1は親HMM比0.05 ft以上、4/5 folds、1000+、hidden-like、p95、worst非悪化を要求する。

## 次

本実験は再開しない。新exp324相当はexp338 successor chainの二段階PASS後に新番号でのみ作成できる。
