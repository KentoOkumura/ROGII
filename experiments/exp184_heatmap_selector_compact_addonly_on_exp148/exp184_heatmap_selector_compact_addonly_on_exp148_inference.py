# ---
# jupyter:
#   jupytext:
#     text_representation:
#       extension: .py
#       format_name: percent
#       format_version: '1.3'
#       jupytext_version: 1.19.3
#   kernelspec:
#     display_name: Python 3
#     language: python
#     name: python3
# ---

# %% [markdown]
# # exp184_heatmap_selector_compact_addonly_on_exp148 inference
#
# This experiment first trains an exp148 add-only model with exp184 heatmap selector
# selected-path confidence features. Raw-test selector feature generation is not
# part of the initial train implementation.

# %% [markdown]
# ## Contents
#
# 1. Setup and configuration
# 2. Inference status

# %%
from __future__ import annotations

from IPython.display import display

from settings import EXPERIMENT_NAME, ExperimentPaths, get_nested, load_config


def cfg_get(config, dotted_key, default=None):
    value = get_nested(config, dotted_key)
    return default if value is None else value


paths = ExperimentPaths()
paths.require_kaggle_runtime()
paths.ensure_output_dirs()
config = load_config()

print("Experiment:", EXPERIMENT_NAME)
print("Route:", cfg_get(config, "experiment.route"))
print("Inference mode:", cfg_get(config, "inference.mode"))
print("Selected variant:", cfg_get(config, "inference.selected_variant"))
print("Parent:", cfg_get(config, "lineage.parent"))
print("Selector parent:", cfg_get(config, "lineage.selector_parent"))

display(
    {
        "status": cfg_get(config, "experiment.status"),
        "notes": cfg_get(config, "inference.notes"),
        "expected_train_artifacts": cfg_get(config, "audit.expected_train_artifacts"),
    }
)

# %% [markdown]
# ## 2. Inference status

# %%
print(
    "No submission is generated by the initial exp184 heatmap compact implementation. "
    "If train-side CV is positive, add current-test exp184 heatmap selector feature "
    "generation and saved-booster inference in this same experiment."
)
