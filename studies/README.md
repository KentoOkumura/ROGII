# 調査

このディレクトリは、再現可能な実験に組み込む前のEDA、プロトタイプ、分析スクリプト、生の表・図に使います。

調査内容をスコア計算に使う段階になったら、関連ロジックを `experiments/` または `src/` に移してください。

完了した調査の結論はここを正にせず、対象が単一実験でも `docs/surveys/` にメタデータ付きレポートとして記録します。`docs/surveys/README.md`が調査結果を探す入口です。対応するsurveyレポートから、このディレクトリのコードや生出力へリンクしてください。
